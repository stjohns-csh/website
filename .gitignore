# Dependencies (not needed — the site has no build step)
node_modules/

# Netlify local build/cache
.netlify/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
.fuse_hidden*

# Scratch left by a session that could not delete files. Safe to remove.
_TRASH-ME-then-delete-this-folder/
